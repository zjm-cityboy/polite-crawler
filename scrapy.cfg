# 工程设置：scrapy 命令据此加载全局配置（必须有，否则爬虫发现不了）
[settings]
default = news_crawler.settings

# Scrapy 部署配置：scrapyd-deploy 命令据此知道往哪台 Scrapyd 打包部署
# 用法：scrapyd-deploy news_scrapyd（先把 compose 里的 scrapyd 服务跑起来）

[deploy:news_scrapyd]
url = http://localhost:6800/
project = news_crawler
